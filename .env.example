# The VAPID public key, exposed to the browser so it can subscribe to push.
# Public by design — it also appears in wrangler.toml and in the built bundle.
# Generate a pair with `npm run keys`, which writes .vapid.json (gitignored)
# and prints the public half to put here.
VITE_VAPID_PUBLIC_KEY=
